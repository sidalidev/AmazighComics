export function Footer() {
  return (
    <footer className="border-t border-[rgb(var(--border))] py-6">
      <div className="max-w-5xl mx-auto px-6 flex items-center justify-between text-sm text-[rgb(var(--muted))]">
        <div className="flex items-center gap-2">
          <span className="text-lg text-[rgb(var(--primary))]">ⵣ</span>
          <span>Amazigh Comics</span>
        </div>
        <span>ⴰⵣⵓⵍ — Paix</span>
      </div>
    </footer>
  );
}
